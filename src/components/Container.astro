---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className }: Props = Astro.props;
---
<div class={cn('w-full max-w-7xl my-0 mx-auto px-5', className)}>
  <slot />
</div>